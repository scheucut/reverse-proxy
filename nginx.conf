events {
}

http {
    server {
        listen 8080;
        location /users {
            proxy_pass http://udagram-user/api/v0/users;

        }
        location /feed {
            proxy_pass http://udagram-feed:8080/api/v0/feed;
        }
    }
}
